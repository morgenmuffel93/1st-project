var circle = {radius: 20, x: 5, y: 5};
var mousePos = {radius: 0, x: event.pageX, y: event.pageY};

var dx = circle.x - event.pageX;
var dy = circle.y - event.pageY;
var distance = Math.sqrt(dx * dx + dy * dy);

if (distance < circle.size) {
    // collision detected!
}